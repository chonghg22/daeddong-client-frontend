name: daeddong
description: "대똥여지도 / 기저귀갈이대 Flutter 앱"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  go_router: ^13.2.0
  flutter_naver_map: ^1.2.0
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  supabase_flutter: ^2.3.4
  shared_preferences: ^2.2.3
  google_mobile_ads: ^5.1.0
  url_launcher: ^6.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
